import 'package:flutter/material.dart';

class Home extends StatelessWidget {
  Widget build(context) => Scaffold(
        appBar: AppBar(
          title: Text("???"),
        ),
        body: Container(
          child: Text("hey"),
        ),
      );
}
